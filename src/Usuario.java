public class Usuario {
    //gera um numero de conta diferente para cada usuário de maneira lúdica
    int numeroConta = (int)(Math.random()*10000);
    String agencia;
    String nome;
    double saldo;
}
